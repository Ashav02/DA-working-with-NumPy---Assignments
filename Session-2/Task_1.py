#Create a NumPy array called my_scores using np.array() with your last 5 Zomato order ratings (pick any integers between 1 and 5), and print the array.

import numpy as np

my_scores = np.array([4,5,2,1,3,5])

print(my_scores)